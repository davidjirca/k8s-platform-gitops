persistence:
  enabled: true
  storageClass: "local-path"
  size: 10Gi

resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    memory: 1.5Gi

postgresql:
  enabled: true
  image:
    registry: docker.io
    repository: postgres
    tag: "15-alpine"
