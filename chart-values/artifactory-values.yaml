global:
  masterKeySecretName: artifactory-masterkey-secret
  joinKeySecretName: artifactory-joinkey-secret
nginx:
  enabled: false
postgresql:
  enabled: true
artifactory:
  masterKeySecretName: artifactory-masterkey-secret
  joinKeySecretName: artifactory-joinkey-secret
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
